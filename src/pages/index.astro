<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>SABM | The Sovereign Herbarium</title>
    <style>
        /* LUXURY MUSEUM SPECIFICATIONS */
        :root {
            --gold: #D4AF37;
            --obsidian: #1c1917;
            --stone: #fafaf9;
            --glass: rgba(255, 255, 255, 0.8);
        }

        body {
            margin: 0;
            padding: 0;
            background-color: #ffffff;
            font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
            color: var(--obsidian);
            -webkit-font-smoothing: antialiased;
        }

        /* TYPOGRAPHY */
        .serif { font-family: 'Georgia', serif; font-style: italic; }
        .caps { text-transform: uppercase; letter-spacing: 0.3em; font-size: 10px; font-weight: 900; color: #a8a29e; }

        /* NAVIGATION */
        nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 40px 60px;
            border-bottom: 1px solid #f5f5f4;
        }

        .logo { font-size: 24px; font-weight: bold; letter-spacing: -1px; }

        /* GRID SYSTEM */
        .container { max-width: 1200px; margin: 0 auto; padding: 80px 40px; }
        
        header { max-width: 700px; margin-bottom: 100px; }
        header h1 { font-size: 64px; font-weight: 300; line-height: 1.1; margin: 0; }

        .museum-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
            gap: 80px;
        }

        /* THE PEDESTAL CARD */
        .specimen-card { display: flex; flex-direction: column; }

        .pedestal {
            position: relative;
            aspect-ratio: 3 / 4;
            border: 1px solid var(--gold);
            background-color: var(--stone);
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
            transition: all 0.6s cubic-bezier(0.16, 1, 0.3, 1);
        }

        .pedestal img {
            max-width: 80%;
            height: auto;
            mix-blend-mode: multiply;
            filter: contrast(1.1);
        }

        .pedestal:hover {
            transform: translateY(-10px);
            box-shadow: 0 30px 60px rgba(0,0,0,0.05);
            background-color: #fff;
        }

        /* BUTTONS & DETAILS */
        .details { margin-top: 40px; }
        .details h3 { font-size: 32px; margin: 0; font-weight: 400; }
        
        .cta-button {
            margin-top: 30px;
            display: inline-block;
            background-color: var(--obsidian);
            color: white;
            text-decoration: none;
            padding: 18px 40px;
            font-size: 9px;
            font-weight: 900;
            text-transform: uppercase;
            letter-spacing: 0.2em;
            transition: background 0.3s;
        }

        .cta-button:hover { background-color: var(--gold); }
    </style>
</head>
<body>

    <nav>
        <div class="logo">SABM</div>
        <div class="caps">
            <span style="margin-right: 30px;">Taxonomy</span>
            <span style="margin-right: 30px;">Oracles</span>
            <span>Heritage</span>
        </div>
    </nav>

    <div class="container">
        <header>
            <div class="caps" style="margin-bottom: 20px;">Established 2026</div>
            <h1>The <span class="serif">Sovereign</span> Herbarium.</h1>
        </header>

        <div class="museum-grid">
            <div class="specimen-card">
                <div class="pedestal">
                    <img src="https://upload.wikimedia.org/wikipedia/commons/thumb/3/30/Sceletium_tortuosum_botanical.jpg/800px-Sceletium_tortuosum_botanical.jpg" alt="Sceletium Illustration">
                </div>
                <div class="details">
                    <h3>Kanna</h3>
                    <div class="caps" style="color: #d6d3d1; margin-top: 5px;">Sceletium tortuosum</div>
                    <div style="width: 40px; height: 1px; background: #e7e5e4; margin: 30px 0;"></div>
                    <a href="#" class="cta-button">Connect with Farmers</a>
                </div>
            </div>

            <div class="specimen-card">
                <div class="pedestal">
                    <img src="https://upload.wikimedia.org/wikipedia/commons/1/1a/Agathosma_betulina_-_Köhler–s_Medizinal-Pflanzen-155.jpg" alt="Buchu Illustration">
                </div>
                <div class="details">
                    <h3>Buchu</h3>
                    <div class="caps" style="color: #d6d3d1; margin-top: 5px;">Agathosma betulina</div>
                    <div style="width: 40px; height: 1px; background: #e7e5e4; margin: 30px 0;"></div>
                    <a href="#" class="cta-button">Connect with Farmers</a>
                </div>
            </div>
        </div>
    </div>

</body>
</html>
