---
// This code is "Plain JavaScript" - it requires no installs.
import topoData from "../data/za-provinces.topojson";
import { getCoords } from "../utils/mapCoords";
import farmData from "../data/farms.json";

const { slug } = Astro.props;

// Access the map data directly from the JSON object
const objectKey = Object.keys(topoData.objects)[0];
const features = topoData.objects[objectKey].geometries;

const getMarkerColor = (type: string) => {
  const colors: Record<string, string> = {
    'organic': '#77DD77',
    'commercial': '#FDFD96',
    'wild': '#A8E4A0',
