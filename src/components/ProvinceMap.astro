---
// This removes all external imports during build to confirm if Sevalla 
// can handle the data files.
import farmData from "../data/farms.json";
// We will mock the provinces for this test
const geometries = []; 
const { slug } = Astro.props;
---
