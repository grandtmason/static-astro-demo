{
  "name": "static-astro-demo",
  "version": "0.0.1",
  "scripts": {
    "build": "astro build",
    "prebuild": "node -e 'console.log(\"Memory limit: \" + process.memoryUsage().heapTotal / 1024 / 1024 + \" MB\")'"
  },
  "dependencies": {
    "astro": "^4.0.0",
    "d3-geo": "^3.1.0",
    "topojson-client": "^3.1.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
