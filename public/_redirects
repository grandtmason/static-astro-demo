# 1. Bypass: Force these paths to be treated as static files
/styles/* /styles/:splat 200
/_astro/* /_astro/:splat 200
/data/* /data/:splat  200

# 2. Catch-all: Route everything else to the index page for SPA-like behavior
/* /index.html 200
